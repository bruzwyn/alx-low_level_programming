#include <stdlib.h>
#include <ctype.h>
/**
 * main -  Entry point
 *
 * Return: 0 on success
 */
int main(void)
{
int lowerCase = '97';
while (lowerCase <= '122')
{
putchar(lowercase);
lowerCase++;
}
putchar('\n');
return (0);
}
