#include <stdlib.h>
#include <ctype.h>
/**
 * main -  program that prints the alphabet in lowercase, followed by a new line.
 *
 * Return: 0 on success
 */
int main(void)
{
int lowerCase = 'a';
while (lowerCase <= 'z')
{
putchar(lowercase);
lowerCase += 1;
}
putchar('\n');
return (0);
}
